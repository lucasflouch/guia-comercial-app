import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.guiacomercial.miapp',
  appName: 'guia-comercial-app',
  webDir: 'dist'
};

export default config;
